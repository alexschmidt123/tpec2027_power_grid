# Recorded offline training time

See offline_training_times.csv and offline_training_times.json. Values are hours; DAD excludes preceding Fixed training. Step-DAD reuses the matching DAD model; Random/Myopic require no offline training. IEEE9 seed101 uses FASTER A100 and seeds202/303 RTX5080; IEEE14 uses Grace A100. Aggregate durations summarize the actual workflow, not a single-hardware benchmark. Source-run paths are preserved in JSON.
